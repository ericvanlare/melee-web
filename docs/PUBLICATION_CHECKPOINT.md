# Publication checkpoint after the runtime handoff

The runtime work has stopped and PR [#79](https://github.com/ericvanlare/melee-web/pull/79)
is merged. This September 25, 2026 UTC review uses main
`60372b468dbe98f2b73bcc690a4ed35273eead91` as its source baseline and
`398044147b012bcc15a7a525ef7621e05de297e4` as the local preparation merge.
Neither is a claim that the final publication commit has been frozen. The
repository remains private. The preparation PR, its CI and the final main
merge must be included in the last delta before visibility changes.

This continues the [earlier pre-freeze preparation](PUBLICATION_PRE_FREEZE.md).
The [publication checklist](PUBLIC_REPOSITORY_CHECKLIST.md) and
[cutover procedure](PUBLICATION_CUTOVER.md) remain authoritative. Record the
eventual frozen SHA, ref inventory and final evidence in
[issue #2](https://github.com/ericvanlare/melee-web/issues/2), avoiding another
source commit merely to record the CI generated by the preceding commit.

## Reviewed material

- [Source and reachable history](evidence/publication-freeze-history-v1.json):
  the refreshed root inventory adds no finding keys to the previously reviewed
  historical images, renderer seeds and synthetic path fixtures. New reachable
  files are text; no new raw game asset or generated binary fixture was found.
- [GitHub discussion surfaces](evidence/publication-freeze-discussions-v1.json):
  issue and PR text, comments, events, reviews, releases and recognized
  attachment references have no recognized secret or non-runner personal-path
  findings. The receipt states the API and pattern limits.
- [Actions logs and artifacts](evidence/publication-freeze-actions-v1.json):
  prior inspections are reused only with verified byte hashes and matching
  archive/run identities. New archives are scanned without extraction or
  execution. Previously expired unavailable payloads remain outside the result.
- [Controls and source-provenance delta](evidence/publication-freeze-controls-provenance-v1.json):
  new original startup/audio fixtures and Dolphin observer changes retain their
  source boundaries. GitHub features unavailable on the private plan remain
  cutover gates, rather than being described as active.

Raw responses, downloads, helper scripts and detailed reports remain ignored
under `work/publication-freeze-20260925/`. The sanitized receipts bind their
identities and limits. These checks establish no universal security clearance,
third-party permission, gameplay equivalence, pixel, PCM or performance result.

The source baseline passed [Verify run 36078461171](https://github.com/ericvanlare/melee-web/actions/runs/36078461171).
PR #79 also passed its own exact-head CI; its runtime evidence remains in the
[audio/memory startup receipt](evidence/source-lbaudio-memory-startup-v1.json)
and [current status](../STATUS.md). The unresolved equivalence investigation
does not prevent publishing source with its existing experimental description.
It remains a separate development task. This publication PR changes documents
and audit receipts, so its local validation is content, JSON, link/path and diff
checking; it does not rerun gameplay experiments.

## Public and private boundaries

Publish the reviewed source, build/reference tools, synthetic tests and
sanitized evidence under the [existing license scope](../LICENSE_SCOPE.md).
The newly added files do not silently enter the MIT allowlist. Preserve the
recovered-source, generated-data, audio and separate Dolphin GPL boundaries.
The [owner-accepted assessment](PUBLICATION_PROVENANCE_ASSESSMENT.md) remains
the publication decision; this checkpoint does not expand that decision into
a blanket license or permission finding.

Keep disc images, extracted assets, raw captures, saves and generated native
binaries in ignored local inputs or controlled private storage. Keep deployment
credentials in secret storage. A separate private operations repository can be
added if deployment work needs one; it is not a prerequisite for publishing
this source repository. Source publication does not deploy a player or release
a native binary.

## Final integration and cutover

1. Review and merge the preparation PR after its exact head passes CI. Inspect
   its new Actions archives, then freeze the resulting main SHA and remote
   branches, tags and available PR refs. Verify the committed-tree guard,
   history delta, discussion edits and final main CI, retaining all failures.
2. Record the owner's visibility decision against that frozen inventory.
   Recheck for new remote content immediately before changing visibility.
3. Follow the cutover procedure to stop Actions writers, retain cache metadata,
   delete disposable private-era caches and verify zero remain. No cache
   payload is covered by the Actions archive scan.
4. During the authorized public transition, apply and read back main protection,
   required CI, fork approval, read-only token permissions and full-SHA action
   pinning. Enable and verify private reporting and maintainer notifications,
   then update SECURITY's availability statement and re-enable reviewed CI.
5. Record the actual visibility, controls and CI receipt in issue #2. Close it
   only when these remaining gates have been completed or explicitly resolved.

Further commits, run attempts, uploads or discussion edits extend the audit
delta. A stopped agent and an earlier green build do not freeze GitHub state.
