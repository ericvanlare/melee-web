# Current runtime holdout evidence — 2026-09-19

This document records the approved current-runtime holdout campaign for issue #33. The first campaign stopped after its failed timing slot; the remaining three timing slots were never started and are permanently unstarted for this freeze. It does not close the historical September 12 causal gate, and it does not claim a current-runtime timing pass.

The owner approved one fixed campaign per untouched holdout after freezing the PR38 runtime, seed, and protocol. The approval and freeze receipt is `work/issue33/holdouts/approval-and-runtime-freeze.json` (`b7a30e9573125b40c6f89dd05894ef15db4e1d82c33c1bd10d594566e05fae36`), and the role-preserving execution plan is `work/issue33/holdouts/holdout-execution-plan.json` (`ef6d3b92af023bdc15b591571f17072d435a17dd2cdd8ca0373c4947c4c1a377`). The frozen runtime is commit `5c6929a63e419973291725006542f1d3fce777cc`, with the 626-record seed `b935fd741f029cd12afb5ac584ef943e51bb00c34c689ae44b1e75d6ae81a8ce`. The timing profile is Chrome/153.0.8010.50 on the Apple M4 test host, headed at 640×480 with a 2× backing scale and focus emulation disabled; its receipt is `work/issue33/holdouts/browser-profile.json` (`70adc74d3f122452d8863d281f8225edb4d57de6ceea07973b49f3cf17ba8efc`).

The two reserved source/reference identities are:

- `ucf-holdout-277c7e0e`: Marth/Marth on Battlefield, exact paired reference length 7,347 frames. Reference A and B are retained under `work/issue33/holdouts/ucf-holdout-277c7e0e-prepared/evaluation/`; the result is explicitly `bounded_cap_exhausted`, so it is not a complete original match and is not gold-admitted.
- `ucf-holdout-feeb4d6d`: Falco/Falco on Final Destination, exact paired reference length 5,953 frames. Reference A and B are retained under `work/issue33/holdouts/ucf-holdout-feeb4d6d-reviewed/evaluation/`; the source discovery completed at the match end, but the result is still not gold-admitted.

The retained state and timer reports have no first divergence and report the declared checks as passing. Both timer audits pass with exact reference binding. The original draw audits report 7,341/7,341 unchanged observed traversals across 7,347 source ticks for Marth and 5,948/5,948 across 5,953 source ticks for Falco; 6 and 5 source ticks were not drawn respectively. These are declared-state and source-draw observations only: performance, pixels, audio, and gold admission were not evaluated. The covered state checks are entry data, inputs, RNG, match clock, fighters, PAD state, and frame count.

Two earlier 180-frame development state preflights remain separate evidence. Both completed with `performance: not_evaluated`, `pixels: not_compared`, and an incomplete source match. They do not convert either holdout into a development input and do not supply timing acceptance.

The fixed timing matrix is one unprofiled cold/warm pair per holdout, with a 300-second slot timeout and unchanged hard thresholds: 33.333 ms for browser gaps and native hard callbacks, plus the 16.667 ms native target. Only `01-marth-cold` has been consumed. Its report covers 7,347 source updates, draws, and callbacks and fails on exactly two live pipelines created (`livePipelinesCreated: 2`, `livePipelinesQueued: 0`). The worst browser callback is 25.060 ms; the worst native callback is 17.305 ms, which is one native target miss but below the 33.333 ms hard threshold. It has zero browser hard gaps, zero native hard callbacks, zero audio underrun/overflow frames, and no browser errors. The attempt is failed acceptance evidence. `02-marth-warm`, `03-falco-cold`, and `04-falco-warm` were never started and are permanently unstarted for this freeze.

A retained supplemental resource event records one creation at runtime frame 3889 / source frame 3754, with `created_delta: 1` and `queued_delta: 0`. It is aggregate-only: no descriptor hash or source draw owner is retained, and no earlier creation event is retained. It cannot assign ownership or retroactively classify the historical September 12 operation.

Preparation artifacts remain part of the record. The first Marth preparation failed before launch because it referenced the missing `tools/extract_disc_file.py` path and emitted no runnable recipe. The earlier Falco preparation wrapper was explicitly unlaunched and superseded. The reviewed preparation wrapper was also unlaunched at preparation time; `run_reference_pipeline` later launched that reviewed bundle for the retained complete reference evaluation. The old prepared Falco bundle was not used for that evaluation. These preparation records are not timing or gameplay passes.

After the stopped timing campaign, a read-only IndexedDB recovery recovered the exact pipeline cache DB and WAL from the copied failed-run profile. The Dawn driver cache files were retained separately and excluded. The review was eligible with exactly two new descriptors (`dfe90cff` and `f285d8c5`), zero payload conflicts, and zero missing base rows. The merged candidate seed is `work/issue33/marth-missing-pipelines/two-descriptor-seed.db` (`4bdb7c4a3e906d907d066f0c65041d7eb3472dc25c9d8c98be6e4946fdce560f`); its preservation receipt records all 626 prior rows preserved field-for-field and 2 appended rows. Focused seed/review validation passed 11 tests. The corrected retained development Marth state capture completed all 7,347 updates/draws with an exact timer match, zero live pipeline creation/queue counts, native/browser worst metrics of 14.350/24.800 ms, and no audio or focus faults. A helper had hardcoded the holdout role; the corrected receipt binds the actual capture as `role: development`, while the original mislabeled state-gate remains retained. Because this state capture overlapped public build and full-suite work, its performance field is `not_evaluated` and it is not timing acceptance; the stopped holdout campaign was not rerun. Both Release builds, the production package audit (25 files, 15,239,544 bytes), the 1,006-test full suite (44 optional skips), and 10 headed public-browser checks passed; the public HTTP audit also passed.

The historical cause remains `unresolved_not_reclassified`. The complete machine-readable receipt is `docs/evidence/current-runtime-holdouts-20260919-v1.json`; it records the source, setup, runtime, configuration, execution slots, reports, hashes, recovery, and evidence limits without asserting completion.
